# Copy to .env.local and adjust. Never commit .env.local.

# API (required)
NEXT_PUBLIC_API_URL=http://localhost:4000/api

# Legal Dashboard mode: set to true to show only legal-counsel menu (no Setup, Projects, Tasks, Insurance, etc.)
# After changing, restart the dev server (hard reload is not enough).
NEXT_PUBLIC_LEGAL_APP=true
